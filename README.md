# sentimentalWidget
A small sentiment feedback widget built with React.

## Project structure

The frontend application lives in the `client/` folder. The project was structured this way to keep the codebase ready for an optional backend service if needed.